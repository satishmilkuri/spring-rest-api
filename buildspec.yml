version: 0.2

phases:
  build:
    commands:
      - echo Build started...
      - mvn clean package
  post_build:
    commands:
      - echo Build completed...
artifacts:
  files:
    - target/rest-api-0.0.1-SNAPSHOT.war
  discard-paths: yes